# Ignore node_modules in both client and server
/node_modules

# Ignore package-lock.json in the root if you're not using it there
/package-lock.json

# Ignore logs and temporary files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore .env files for sensitive data (if you have one)
.env

# Ignore build output folder for client (Vite build)
/client_fe/dist
/server_be/dist

# Optional: Ignore the IDE/Editor-specific files (e.g., VS Code, IntelliJ, etc.)
.vscode/
.idea/

# Optional: Ignore other OS-specific files
.DS_Store
Thumbs.db
